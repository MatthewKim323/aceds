# Competition artifact snapshots

Archived copies of generated evaluation JSON for pitch / slides (regenerate in CI or locally to refresh).

- `decision_eval_synthetic.json` — copy of `data_pipeline/processed/decision_eval_synthetic.json` from `23_decision_eval_synthetic.py`.

Headline figures live under `data_pipeline/processed/pitch/`; see [`docs/COMPETITION_METRICS.md`](../COMPETITION_METRICS.md).
